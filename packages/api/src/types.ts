// Placeholder types - Full API implementation coming in future release
export interface ApiConfig {
  // Configuration will be defined when API package is implemented
}

export interface PlaceholderResponse {
  message: string;
}